signKey: "CHANGE_ME_TO_A_RANDOM_32_BYTE_SECRET" # token signing key; change before production use
debugMode: false # debug mode (recommended false in production)
db:
  driver: "sqlite"
  dsn: "./server.db"
  timeZone: "Asia/Shanghai" # setting the time zone fixes the database creation time problem
  showSql: false

  # driver: "mysql"
  # dsn: "root:123@tcp(localhost:3306)/test?charset=utf8mb4"
httpConfig:
  printRequestLog: false
  staticdir: "/app/dist"
  port: ":12345" # api server port

smtpConfig:
  host: "127.0.0.1"
  port: 1025
  username: ""
  password: ""
  encryption: "none" # none ssl/tls starttls
  from: "noreply@example.com"

jobsConfig:
  deviceCheckJob:
    duration: 30

oidc:
  enabled: false
  providerName: "oidc"
  issuer: ""
  clientId: ""
  clientSecret: ""
  # If empty, callback URL is inferred as "<current_origin>/admin/auth/oidc/callback"
  redirectUrl: ""
  scopes:
    - "openid"
    - "profile"
    - "email"
  bindByEmail: true
  autoCreateAdmin: false
  stateTtlSeconds: 180
  ticketTtlSeconds: 180
  successRedirect: "/login"
  failureRedirect: "/login"
  subjectClaim: "sub"
  emailClaim: "email"
  nameClaim: "name"
  pictureClaim: "picture"
  prompt: ""
  allowedEmailDomains: []

oauth:
  # Custom OIDC provider example
  # providers:
  #   - type: "oidc"
  #     name: "company-sso"
  #     displayName: "Company SSO"
  #     enabled: false
  #     issuer: "https://sso.example.com"
  #     clientId: ""
  #     clientSecret: ""
  #     redirectUrl: ""
  #     scopes: ["openid", "profile", "email"]
  #     bindByEmail: true
  #     autoCreateAdmin: false
  #
  # Google provider example
  #   - type: "google"
  #     name: "google"
  #     displayName: "Google"
  #     enabled: false
  #     clientId: ""
  #     clientSecret: ""
  #
  # GitHub provider example
  #   - type: "github"
  #     name: "github"
  #     displayName: "GitHub"
  #     enabled: false
  #     clientId: ""
  #     clientSecret: ""
  providers: []
